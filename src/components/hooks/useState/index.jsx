import "../";
export const counter = () => {
  return (
    <div
      className="container state-container"
      style={{
        textAlign: "center",
      }}
    >
      <h1>useState Hooks!</h1>
    </div>
  );
};
